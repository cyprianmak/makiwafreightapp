# Example environment variables (DO NOT put real secrets here).
# Preferred: set DATABASE_URL to the full connection string:
# DATABASE_URL=postgresql://username:password@host:5432/database

# Or set the individual variables used by the app:
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_HOST=your_db_host
DB_DATABASE=your_db_name
DB_PORT=5432

# Web server port (do NOT set to 5432). Render provides PORT automatically.
# PORT=10000